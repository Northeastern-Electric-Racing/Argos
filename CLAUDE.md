# CLAUDE.md

> Placeholder — a fuller version of this file will replace it. For now, only the agent-skills block below is authoritative.

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `Northeastern-Electric-Racing/Argos` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`), created lazily by `/triage`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: per-component `CONTEXT.md` and `docs/adr/` under `angular-client/`, `scylla-server/`, `siren-base/`, `charybdis/`, indexed by `CONTEXT-MAP.md` at the root; system-wide decisions live in the root `docs/adr/`. See `docs/agents/domain.md`.
