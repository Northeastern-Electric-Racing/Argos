services:
    scylla-server-rust:
        environment:
            - PROD_SIREN_HOST_URL=host.docker.internal:1883
        extra_hosts:
            - "host.docker.internal:host-gateway" # for external siren
